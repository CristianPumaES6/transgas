# BACKEND


./backend/src/config/server.config.ts

Cambiamos la configuracion del server.


Colocamos 

nest start

Copiamos la carpeta dist y el archivo package

Cambiamos de branch y pegamos eso en el backend

git push

nos vamos al server



pm2 list

pm2 delete main 


git pull

npm install


pm2 start main.js


# FRONTEND

./backend/src/config/server.config.ts

Cambiamos la configuracion del server.

ng build --prod

firebase deploy

y se deploya en el transgas.web.app


Copiamos la carpeta dist y el archivo package


nos movemos al branch al de produccion y lo pegamos.

git push

nos vamos al server

git pull  

npm install


// AQUI FALTA ENLAZAR EN PROYECT NEST A ANGULAR.